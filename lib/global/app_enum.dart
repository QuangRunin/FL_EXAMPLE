enum CacheManagerKey {
  token,
  language,
  userInfo,
  passBoarding,
  fcmToken,
}

enum SocialType { normal, google, facebook, apple }

enum VerifyType { verifyAccount, newPassword }

enum SettingTitleType {
  notice,
  setting,
  language,
  helpCenter,
  signOut,
  editProfile,
  changePassword,
  contact,
  instruct,
  privacy,
  termAndRegisterCondition,
  privacyPolicy,
  deletePrivacyData,
}

enum GrammarLevelType {
  primaryLevel,
  secondaryLevel,
  highLevel,
}

enum CategoryStyleType { style1, style2, style3, style4 }

enum CategoryType { culture, interview, news }

enum ExamType { alphabet, tip, grammar, lesson, exam, video }

enum NotificationSelectType { markRead, delete }

enum ExamQuestionFileType { audio, image }

enum ActiveDayType { thisWeek, thisMonth, threeMonths, sixMonths }

enum QuizStatusType { processing, done }

enum QuizType { miniExam, exam }
